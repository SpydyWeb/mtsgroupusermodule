import dashboard from './dashboard';
import { vendormodule, customermodule, usermodule } from './utilities';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, usermodule, vendormodule, customermodule]
};

export default menuItems;
